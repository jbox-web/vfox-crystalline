#!/usr/bin/env bash
#MISE description="Run plugin tests - install and execute tool"
set -euo pipefail

mise plugin link --force crystalline .
mise cache clear
mise install crystalline@0.18.0

# Capture and display the output while also verifying it
output=$(mise exec "crystalline@0.18.0" -- "crystalline" --version)
echo "crystalline output: ${output}"

if echo "${output}" | grep -q '0.18.0'; then
  echo "✓ Tests passed"
else
  echo "✗ Test failed: expected version crystalline@0.18.0"
  exit 1
fi
